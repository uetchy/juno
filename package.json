{
  "name": "juno",
  "productName": "Juno",
  "version": "0.3.1",
  "private": true,
  "description": "Jupyter Notebook stays on macOS menubar.",
  "main": ".",
  "license": "MIT",
  "repository": "uetchy/juno",
  "electronVersion": "1.7.8",
  "scripts": {
    "test": "prettier --write {test,scripts}/*.js *.js --single-quote",
    "coverage": "nyc report --reporter=text-lcov",
    "start": "electron .",
    "build": "electron-packager . $npm_package_productName --out=dist --asar --platform=darwin --arch=x64 --electron-version=$npm_package_electronVersion --icon=./assets/app.icns --overwrite",
    "postbuild": "node scripts/post-build-mac.js",
    "release": "npm-run-all test build"
  },
  "dependencies": {
    "extend": "^3.0.1",
    "yargs": "^9.0.1"
  },
  "devDependencies": {
    "ava": "^0.22.0",
    "coveralls": "^3.0.0",
    "electron": "^1.7.9",
    "electron-packager": "^9.1.0",
    "eslint": "^4.9.0",
    "eslint-config-airbnb-base": "^12.0.2",
    "eslint-plugin-import": "^2.7.0",
    "npm-run-all": "^4.1.1",
    "nyc": "^11.2.1",
    "plist": "^2.1.0"
  }
}
